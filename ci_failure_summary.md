# CI Failure Summary — PR #12209 (Build #30381)

**Overall: 27,740 passed | 181 failed | 10,416 skipped**

## RMSE Numerical Tolerance Failures

| # | Model | Test Type | Error | GPUs Affected | Status |
|---|-------|-----------|-------|---------------|--------|
| 1 | gemma | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 1.37-1.55, decoder: 0.17) | A30, B200 | Causal mask mismatch: `GemmaADAttention` passes `is_causal=True` to `torch_attention`, but the test passes `attention_mask=None` to HF eager attention (which skips masking entirely). Fix: pass an explicit causal mask to the HF attention call in the test, matching the approach used for llama3/qwen2. |
| 2 | llama3 | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 1.04-1.08, decoder: 0.15-0.16) | A30, B200 | Causal mask mismatch: test passes `attention_mask=None` to HF eager attention (no mask applied), but AD custom model uses `is_causal=True`. Fix: build an additive causal mask and pass it to HF attention calls, same as the qwen2 fix in commit `124fe8b4b1`. |
| 3 | mistral | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 1.04-1.08, decoder: 0.15-0.16) | A30, B200 | Causal mask mismatch: test passes `attention_mask=None` to HF eager attention (which skips masking entirely in transformers 4.57+), but the AD custom `MistralAttention` uses `is_causal=True` in `torch_attention`. Fix: pass an explicit causal mask to the HF attention call in `test_mistral_modeling.py`. |
| 4 | mistral3 | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 1.04-1.08, decoder: 0.15-0.16) | A30, B200 | Same root cause as llama3/mistral/smollm3: causal mask mismatch where CI-generated test passes `attention_mask=None` to HF eager attention (no masking) but the AD model uses `is_causal=True` in `torch_attention`. Fix: pass an explicit causal mask to the HF reference call. |
| 5 | granite | attention_equivalence | RMSE ratio exceeds tolerance (attn: 1.06-1.11) | A30, B200 | Test bug: HF eager attention with `attention_mask=None` skips causal masking, but custom `GraniteAttention` hardcodes `is_causal=True` in `torch_attention` call. Fix: pass a causal attention mask to the HF reference in the test. Same root cause as llama3/mistral/mistral3/smollm3 failures. |
| 6 | olmo3 | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 0.87-0.96, decoder: 0.60-0.67) | A30, B200 | Pre-existing bug in olmo3 custom model: `Olmo3Attention.forward` passes `is_causal=True` to `torch_attention`, but HF's `eager_attention_forward` with `attention_mask=None` skips causal masking entirely, causing a fundamental output mismatch. Not a regression from this branch. |
| 7 | smollm3 | attention_equivalence, decoder_layer_equivalence | RMSE ratio exceeds tolerance (attn: 1.03-1.08, decoder: 0.15-0.16) | A30, B200 | Causal mask mismatch: custom `SmolLM3Attention` passes `is_causal=True` to `torch_attention`, but test passes `attention_mask=None` to HF eager attention (which skips masking entirely). Same root cause as llama3/gemma/mistral/granite failures; fix by supplying an explicit causal mask to the HF reference call. |
| 8 | granite_moe_hybrid | attention_decoder_layer_nope_equivalence, moe_equivalence | NaN RMSE (rmse_diff=nan, rmse_ref=nan) | A30, B200 | Root cause: HF `GraniteMoeHybridParallelExperts` uses `torch.empty()` for weight init, leaving MoE expert weights uninitialized (NaN); test copies these NaN weights to the custom model via `load_state_dict`, so both HF ref and custom output are NaN. Fix: explicitly initialize HF MoE weights (e.g., `nn.init.normal_`) before copying. |
| 9 | glm_moe_dsa | decoder_layer_numerical_equivalence | NaN RMSE (rmse_diff=nan, rmse_ref=nan) | A30, B200 | Root cause: HF ref `_HFRefTopkRouter` uses `torch.empty()` for gate weight without initialization; the decoder layer test (unlike the standalone MoE test) does not reinitialize these weights with `torch.randn_like`, so uninitialized GPU memory (potentially NaN/Inf) propagates through sigmoid routing, producing NaN outputs in both ref and custom model. Fix: add `hf_layer.mlp.gate.weight = nn.Parameter(torch.randn_like(hf_layer.mlp.gate.weight))` before copying weights. |

## Other Failures

| # | Category | Test(s) | Error | GPUs Affected | Status |
|---|----------|---------|-------|---------------|--------|
| 10 | SmolLM3 padding_idx | test_smollm3_full_model_equivalence, test_smollm3_model_can_be_exported, test_smollm3_gqa_structure, test_smollm3_nope_layer_pattern, test_smollm3_state_dict_keys | `Padding_idx must be within num_embeddings` | A30, B200 | Test config bug: `_create_small_config()` sets `vocab_size=1000` but does not override `pad_token_id`, which defaults to `128004` in `SmolLM3Config`; `SmolLM3Model.__init__` passes `config.pad_token_id` as `padding_idx` to `nn.Embedding(1000, ...)`, triggering the assertion. Fix: add `pad_token_id=0` (or `None`) to the `_create_small_config()` call. |
| 11 | Qwen3Next KeyError | test_qwen3_next_attention, test_qwen3_next_decoder_layer | `KeyError: None` | A30, B200 | Test bug: tests create HF `Qwen3NextAttention`/`Qwen3NextDecoderLayer` directly (not via `PreTrainedModel.__init__`), so `config._attn_implementation` stays `None`. In `Qwen3NextAttention.forward`, the check `if self.config._attn_implementation != "eager"` is True for `None`, causing `ALL_ATTENTION_FUNCTIONS[None]` → `KeyError`. Fix: set `config._attn_implementation = "eager"` before creating HF reference attention/decoder layer instances. |
| 12 | Qwen3Next SDPA | test_qwen3_next_full_model | `Qwen3NextForCausalLM does not support scaled_dot_product_attention` | A30, B200 | Test bug: `HFModel(config)` calls `PreTrainedModel.__init__` which auto-selects `_attn_implementation="sdpa"`, but Qwen3Next's SDPA path raises `ValueError` at forward time. Fix: pass `attn_implementation="eager"` in `_create_small_config()` (e.g., `config._attn_implementation = "eager"`) or use `HFModel._from_config(config, attn_implementation="eager")`. |
| 13 | OpenELM config | test_openelm_* (all 5 tests) | `'_TestOpenELMConfig' object has no attribute 'initializer_range'` | A30, B200 | Test bug: `_TestOpenELMConfig` in `_create_small_config()` is missing `initializer_range`. The custom model's `OpenELMPreTrainedModel._init_weights()` reads `self.config.initializer_range`, which is called during `post_init()` in `OpenELMModel.__init__` and `OpenELMForCausalLM.__init__`. Fix: add `initializer_range=0.02` to the `_TestOpenELMConfig(...)` constructor call in `_create_small_config()`. |
| 14 | Chat template str/list | test_ad_build_small_single (Mixtral, Phi-3, Qwen2.5, Qwen3-30B, Nemotron-Nano) | `TypeError: can only concatenate str (not "list") to str` / jinja2 UndefinedError | A30, B200, H100_PCIe | Two bugs interact: (1) `prepare_queries()` in `build_and_run_ad.py` wraps every plain-string query with a `messages` key when the tokenizer has a chat template, causing `ADInputProcessor.__call__` to enter the `"messages" in inputs` branch; (2) inside that branch, `ADInputProcessor.__call__` unconditionally normalizes `message["content"]` from `str` to `[{"type": "text", "text": str}]` (list-of-dicts format for multimodal Llama4), which breaks text-only models whose Jinja2 chat templates expect `content` to be a plain string (Mixtral: `str + list`, Qwen3: `.startswith()`, Nemotron: `.replace()`). Fix: guard the content normalization with a check for multimodal processors (e.g., `hasattr(self.processor, 'image_processor')`) so text-only models keep `content` as a string. |
| 15 | ExperimentConfig validation | test_ad_guided_decoding_regex, test_ad_trtllm_sampler | `ValidationError: prompt.queries — Input should be a valid string` | A30, B200, H100_PCIe | |
| 16 | trtllm-bench exit code | test_trtllm_bench (TinyLlama, torch-compile/torch-opt) | `SystemExit(1)` | A30, B200, H100_PCIe | |
| 17 | Llama4 VLM patch | test_build_run_llama4_vlm | `ValueError: Unknown patch: hf_llama4_vision` | A30, B200 | PR #12209 deletes `tensorrt_llm/_torch/auto_deploy/models/patches/llama4.py` (which registered `hf_llama4_vision` and `hf_llama4_moe` patches) and replaces it with a custom modeling approach in `models/custom/modeling_llama4.py`, but the old test `tests/unittest/auto_deploy/singlegpu/models/test_llama4_vlm_patch.py` was not removed or updated. Fix: delete `test_llama4_vlm_patch.py` (the new `test_llama4_modeling.py` replaces it) or, if the VLM torch.cond export path is still needed, re-register the `hf_llama4_vision` patch in the new custom modeling module. |
| 18 | MiniMax M2 collection | test_minimax_m2_patches | `collection failure` | A30, B200, H100_PCIe | Collection-time crash: `hf_id_to_local_model_dir("MiniMaxAI/MiniMax-M2")` is called inside `pytest.param()` decorator (line 71), which executes at module import/collection time. `MiniMaxAI/MiniMax-M2` is not in `HF_ID_TO_LLM_MODELS_SUBDIR` (in `tests/test_common/llm_data.py`), so the fallback checks for a `MiniMax-M2` directory under `LLM_MODELS_ROOT`; in CI this directory does not exist, raising `ValueError` and crashing collection. Fix: either (1) add `"MiniMaxAI/MiniMax-M2"` to `HF_ID_TO_LLM_MODELS_SUBDIR` in `llm_data.py`, or (2) move the `hf_id_to_local_model_dir` call inside the test function body (like `test_eagle.py` does) with a `pytest.skip` guard when the model is not found. |
| 19 | CudaGraph batch size heuristic | test_medium_max_batch_size_caps_heuristic, test_large_max_batch_size_includes_all_heuristic_values | `assert 17 in [64, 48, 32, 16, 1]` | A30, B200, H100_PCIe | |
| 20 | Multi-GPU thread leak | test_ad_build_small_multi (Meta-Llama-3.1-8B) | `Test leaked threads` | B200, H100 | |
| 21 | ONNX export schema | test_ad_export_onnx (Qwen2.5-3B) | `SchemaError: rope_with_explicit_cos_sin already registered` | H100_PCIe | |
| 22 | Perf benchmark | test_perf (deepseek_r1_distill_qwen_32b) | `CalledProcessError` | H100_PCIe | |
| 23 | Test terminated | unittest/_torch/misc, multigpu/smoke | `Test terminated unexpectedly` | B200 | |
