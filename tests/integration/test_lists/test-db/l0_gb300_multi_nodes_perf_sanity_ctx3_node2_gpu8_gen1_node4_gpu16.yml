version: 0.0.1
l0_gb300_multi_nodes_perf_sanity_ctx3_node2_gpu8_gen1_node4_gpu16:
- condition:
    ranges:
      # 3 ctx workers each with 2 nodes and 8 GPUs
      # 1 gen worker with 4 nodes and 16 GPUs
      system_gpu_count:
        gte: 40
        lte: 40
    wildcards:
      gpu:
      - '*gb300*'
    terms:
      stage: post_merge
      backend: pytorch
  tests:
  # deepseek-v4-pro-dspark agentx con1456.
  # e2e only -- see the sibling ctx2_node2_gpu8_gen1_node2_gpu8 list for why
  # agentx gen_only needs the gen_only_no_context flavor and is left to a
  # follow-up.
  - perf/test_perf_sanity.py::test_e2e[disagg_upload-e2e-gb300_deepseek-v4-pro-dspark_agentx_con1456_ctx3_dep8_gen1_dep16_eplb0_dspark5_ccb-NIXL] TIMEOUT (210)
