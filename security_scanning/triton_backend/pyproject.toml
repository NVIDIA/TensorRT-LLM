[tool.poetry]
name = "triton-backend"
version = "0.1.0"
description = ""
authors = ["TensorRT LLM [svc_tensorrt@nvidia.com]"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.10,<3.13"
regex = "^2025.9.1"
fire = "^0.7.1"
tritonclient = {extras = ["all"], version = "^2.60.0"}
transformers = "4.55.0"
pandas = "^2.3.2"
tabulate = "^0.9.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
