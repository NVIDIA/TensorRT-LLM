* Before committing changes, use `pre-commit run -a` command to make sure that there are no style issue.
* Always add tests when adding new features, and always make sure that all test cases can pass before committing.
