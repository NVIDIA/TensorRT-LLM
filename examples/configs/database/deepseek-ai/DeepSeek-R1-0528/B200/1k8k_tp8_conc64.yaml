max_batch_size: 512
cuda_graph_config:
  enable_padding: true
  max_batch_size: 64
print_iter_log: true
kv_cache_config:
  dtype: fp8
  free_gpu_memory_fraction: 0.8
stream_interval: 10
moe_config:
  backend: DEEPGEMM
speculative_config:
  decoding_type: MTP
  max_draft_len: 3
tensor_parallel_size: 8
moe_expert_parallel_size: 8
trust_remote_code: true
max_seq_len: 9416
