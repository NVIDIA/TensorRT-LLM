max_batch_size: 4
cuda_graph_config:
  enable_padding: true
  max_batch_size: 128
print_iter_log: true
kv_cache_config:
  dtype: fp8
  free_gpu_memory_fraction: 0.75
stream_interval: 10
moe_config:
  backend: CUTLASS
speculative_config:
  decoding_type: MTP
  max_draft_len: 3
tensor_parallel_size: 8
moe_expert_parallel_size: 8
trust_remote_code: true
max_num_tokens: 1152
max_seq_len: 2304
