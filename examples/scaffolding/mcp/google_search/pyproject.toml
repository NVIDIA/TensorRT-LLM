[project]
name = "google-search-mcp"
version = "0.1.0"
description = "MCP server for Google web search via SerpAPI"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.9.0",
    "pyyaml>=6.0",
    "uvicorn>=0.30.0",
    "starlette>=0.38.0",
]
