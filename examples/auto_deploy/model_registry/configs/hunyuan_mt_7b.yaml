# Configuration for Tencent Hunyuan-MT-7B (Dense V1)
# Standard dense GQA model with QK norm and Dynamic NTK-Alpha RoPE.
# Uses a custom AutoDeploy model implementation (HunYuanDenseV1ForCausalLM)
# registered locally; no additional overrides needed beyond dashboard_default.
