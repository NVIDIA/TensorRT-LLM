cuda_graph_config:
  enable_padding: true
  batch_sizes:
  - 512

kv_cache_config:
  enable_block_reuse: false
  dtype: auto

moe_config:
  backend: TRTLLM
  use_low_precision_moe_combine: true

enable_attention_dp: true
